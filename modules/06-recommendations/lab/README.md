# Lab assets for 06 Recommendations (to be built)

This folder will hold everything the lab spec in [../README.md](../README.md) references:

- `ollama.http`: the embeddings request (only needed if computing fresh vectors)
- the module 04 trail slice, three target trails, and precomputed embeddings for a standalone start
- `expected-output.md`: the acceptable neighbor sets per target trail (more than one right answer)

Until these land, the lab spec in the module README is the source of truth for what belongs here.
