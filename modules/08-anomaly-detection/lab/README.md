# Lab assets for 08 Anomaly Detection (to be built)

This folder will hold everything the lab spec in [../README.md](../README.md) references:

- `ollama.http`: the embeddings request against `nomic-embed-text`
- one trail's reports from `data/condition-reports.jsonl` (about 40, with the planted washout cluster) and precomputed embeddings
- `expected-output.md`: the expected distance ranking, washout reports on top

Until these land, the lab spec in the module README is the source of truth for what belongs here.
