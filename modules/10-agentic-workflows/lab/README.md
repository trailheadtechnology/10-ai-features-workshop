# Lab assets for 10 Agentic Workflows (to be built)

This folder will hold everything the lab spec in [../README.md](../README.md) references:

- `azure.http`: the full tool-calling round-trip as sequential requests against Azure OpenAI
- tool definitions as JSON and a reference transcript of a complete agent run (mock API fixtures live in `data/mock-apis/`)
- `expected-output.md`: the expected weather-aware itinerary and the washed-out-bridge avoidance check

Until these land, the lab spec in the module README is the source of truth for what belongs here.
