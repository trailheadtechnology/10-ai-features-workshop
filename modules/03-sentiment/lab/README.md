# Lab assets for 03 Sentiment (to be built)

This folder will hold everything the lab spec in [../README.md](../README.md) references:

- `ollama.http`: the classify request against `phi3`
- `azure.http`: the same prompt against Azure OpenAI
- about 20 reviews from `data/gear-reviews.jsonl`, split into easy and hard sets, with reference labels
- `expected-output.md`: the reference labels and the expected disagreement list

Until these land, the lab spec in the module README is the source of truth for what belongs here.
