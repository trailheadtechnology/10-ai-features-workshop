# Lab assets for 02 Extraction (to be built)

This folder will hold everything the lab spec in [../README.md](../README.md) references:

- `ollama.http`: the chat request with the JSON schema payload (`llama3.2`, JSON mode)
- two trip reports from `data/trip-reports/`, one rich in facts and one sparse
- `expected-output.md`: the reference JSON records, with honest nulls for the sparse report

Until these land, the lab spec in the module README is the source of truth for what belongs here.
