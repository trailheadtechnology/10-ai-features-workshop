# Azure Soak Test, 2026-09-03

Deployment: `trailhead-ai-workshop` (eastus2), key1. `gpt-4.1` for F03/F05, `gpt-5.5` for F10.
Runner: `dotnet/complete` for each module, `dotnet run --no-build`.

| Module | Runs | Result |
|---|---|---|
| F00 smoke (local chat, local embed, cloud chat) | 3 | 3/3 on every leg; cloud model `gpt-4.1-2025-04-14` |
| F03 sentiment, gpt-4.1 | 4 | easy 10/10 (one run 9/10, `gr-0014` -> positive); hard 10/10 after relabel below |
| F05 RAG, gpt-4.1 | 4 questions x 3 | 12/12 correct; every citation a real chunk id; EV question refused 3/3 |
| F10 agent default, gpt-5.5, `--yes` | 10 | 10/10 plans; weather -> search -> conditions per trail -> campsites every run; no invented trails; no permit (frontcountry only) |
| F10 washout, request includes "(trail-0117)" | 10 | 10/10 called `get_trail_conditions` on 0117 and flagged/replaced it |
| F10 washout, name only, tag-only search | 10 | 4/10 reached the trail; 6/10 said it could not find it (honest, no fabrication) |
| F10 washout, name only, after name-match fix | 5 dotnet + 1 python + 1 typescript | 7/7 found the trail by name, checked 0117, stated the closure |

## Changes made from the soak

1. `F03 data/reference-labels.json`: `gr-0004` relabeled `positive` -> `mixed`. gpt-4.1 said mixed on
   every run and the review really is split. Ripple: llama3.2 stand-in hard score 7/10 -> 8/10.
   Updated expected-output.md, the three track docs, the http doc, the M1 outline notes, and the
   hand-made M1 deck's slide-30 notes.
2. `F10 search_trails` (dotnet/python/typescript complete + tool-definitions.json + azure.http):
   keywords now match the trail name as well as feature tags. Rationale documented in F10-lab.md,
   expected-output.md step 3, the azure.http step-3 comment, and the M4 outline demo notes.
3. Rain-day spread (3/10 runs put a moderate hike on the 16th) documented in F10 expected-output.md
   step 2 and the M4 outline notes.

## Still open

- Offline fallback for the day the venue network dies: F03 falls back to llama3.2 automatically
  (now a near-tie, which changes the lesson; say so). F10 on llama3.2 is unreliable; the fallback
  is `reference-transcript.md` read aloud.
- Regenerate Foundry key2 after Sept 23 (`instructor.local.md`).
